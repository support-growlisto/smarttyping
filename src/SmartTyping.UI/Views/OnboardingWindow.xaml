<Window x:Class="SmartTyping.UI.Views.OnboardingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:loc="clr-namespace:SmartTyping.UI.Localization"
        Title="{Binding [Onb_Title], Source={x:Static loc:LocalizationManager.Instance}}"
        Height="440" Width="470"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        FontFamily="Segoe UI" FontSize="13">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" FontSize="20" FontWeight="SemiBold"
                   Text="{Binding [Onb_Heading], Source={x:Static loc:LocalizationManager.Instance}}" />
        <TextBlock Grid.Row="1" Margin="0,6,0,16" Foreground="{DynamicResource Brush.SubtleForeground}" TextWrapping="Wrap"
                   Text="{Binding [Onb_Intro], Source={x:Static loc:LocalizationManager.Instance}}" />

        <StackPanel Grid.Row="2">
            <Border Background="{DynamicResource Brush.SurfaceAlt}" CornerRadius="6" Padding="14" Margin="0,0,0,12">
                <StackPanel>
                    <TextBlock FontWeight="SemiBold"
                               Text="{Binding [Onb_Fix_Title], Source={x:Static loc:LocalizationManager.Instance}}" />
                    <TextBlock Margin="0,4,0,0" TextWrapping="Wrap"
                               Text="{Binding [Onb_Fix_Body], Source={x:Static loc:LocalizationManager.Instance}}" />
                </StackPanel>
            </Border>

            <Border Background="{DynamicResource Brush.SurfaceAlt}" CornerRadius="6" Padding="14" Margin="0,0,0,12">
                <StackPanel>
                    <TextBlock FontWeight="SemiBold"
                               Text="{Binding [Onb_Expand_Title], Source={x:Static loc:LocalizationManager.Instance}}" />
                    <TextBlock Margin="0,4,0,0" TextWrapping="Wrap"
                               Text="{Binding [Onb_Expand_Body], Source={x:Static loc:LocalizationManager.Instance}}" />
                </StackPanel>
            </Border>

            <TextBlock Foreground="{DynamicResource Brush.SubtleForeground}" TextWrapping="Wrap"
                       Text="{Binding [Onb_Tray], Source={x:Static loc:LocalizationManager.Instance}}" />
        </StackPanel>

        <Button Grid.Row="3" HorizontalAlignment="Right" Width="120" Padding="0,7"
                Margin="0,16,0,0" IsDefault="True" Click="OnGotItClick" Style="{StaticResource AccentButton}"
                Content="{Binding [Onb_GotIt], Source={x:Static loc:LocalizationManager.Instance}}" />
    </Grid>
</Window>
