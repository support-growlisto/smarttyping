<Window x:Class="SmartTyping.UI.Views.PlaceholderInputWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:loc="clr-namespace:SmartTyping.UI.Localization"
        Title="{Binding [Input_Title], Source={x:Static loc:LocalizationManager.Instance}}"
        SizeToContent="Height" Width="380" MaxHeight="520"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        ShowInTaskbar="False" FontFamily="Segoe UI" FontSize="13">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ItemsControl Grid.Row="0" ItemsSource="{Binding Fields}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <StackPanel Margin="0,0,0,10">
                        <TextBlock Text="{Binding Label}" Margin="0,0,0,3" />
                        <TextBox Text="{Binding Value, UpdateSourceTrigger=PropertyChanged}" Padding="5,4"
                                 Tag="field" />
                    </StackPanel>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,6,0,0">
            <Button Width="90" Margin="0,0,8,0" IsCancel="True" Click="OnCancelClick"
                    Content="{Binding [Edit_Cancel], Source={x:Static loc:LocalizationManager.Instance}}" />
            <Button Width="90" IsDefault="True" Click="OnInsertClick" Style="{StaticResource AccentButton}"
                    Content="{Binding [Input_Insert], Source={x:Static loc:LocalizationManager.Instance}}" />
        </StackPanel>
    </Grid>
</Window>
